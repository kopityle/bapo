apiVersion: v1
kind: Service
#имя, по которому будут находить сервис
metadata:
  name: redis
spec:
#по какой метке искать поды , на которые слать трафик
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379